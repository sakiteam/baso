name: baso
description: La base de tout projet Flutter de Saki !
version: 0.0.1
homepage:

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  # Firebase
  firebase_auth: ^3.2.0
  firebase_core: ^1.10.0
  cloud_firestore: ^3.1.0
  firebase_storage: ^10.1.0
  firebase_messaging: ^11.1.0

  get: ^4.6.1
  uuid: ^3.0.5
  share: ^2.0.4
  http: ^0.13.4
  diacritic: ^0.1.3
  animations: ^2.0.2
  url_launcher: ^6.0.18
  flutter_svg: ^1.0.3
  package_info: ^2.0.2
  universal_io: ^2.0.4
  auto_size_text: ^3.0.0
  store_redirect: ^2.0.1
  delayed_display: ^2.0.0
  shared_preferences: ^2.0.13
  feature_discovery: ^0.14.1
  cached_network_image: ^3.2.0
  smooth_page_indicator: ^1.0.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  # Fonts
  fonts:
    - family: Brandon
      fonts:
        - asset: font/font.otf
